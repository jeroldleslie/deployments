dataDir={{ zookeeper_data_dir }}
clientPort={{ zookeeper_client_port }}
maxClientCnxns=0
tickTime=2000
initLimit=5
syncLimit=2

{% for host in groups['storm-zookeeper-group'] %}
server.{{ hostvars[host].zoo_id }}={{ host }}:{{ zookeeper_leader_port }}:{{ zookeeper_election_port }}
{% endfor %}

