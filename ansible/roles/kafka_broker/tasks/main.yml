---
- name: download tar 
  get_url: url={{ kafka_tgz_url }} dest=/tmp/kafka.tar.gz

- name: Create kafka directory
  file: path=/opt/kafka state=directory

- name: Decompress tar
  command: tar xfz /tmp/kafka.tar.gz -C /opt/kafka --strip-components=1


